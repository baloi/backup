Wnit github.com/baloi/contactsn first made the module:
$ go mod init github.com/baloi/contacts/app

Then we wrote the source...

The main function is in app.go:

    func main() 


To make this work:

$ go build
$ ./app

The executable is named "app" because that is what we named our module
